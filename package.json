{
  "name": "esgc-root",
  "scripts": {
    "fastapi": "cd fastapi && uvicorn fast:app --host 0.0.0.0 --port 8000",
    "next": "cd ESG && npm run dev",
    "dev": "concurrently \"npm run fastapi\" \"npm run next\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "npm-run-all": "^4.1.5"
  }
}
